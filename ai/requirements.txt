# AI layer dependencies.
#
#   pip install -r ai/requirements.txt
#
# Or, if you use uv (much faster):
#   uv pip install -r ai/requirements.txt

# Snowflake driver. The [pandas] extra brings fetch_pandas_all(), which every
# script here relies on for turning result sets into DataFrames.
snowflake-connector-python[pandas]>=3.6,<4

# OpenAI SDK v1.x — the v0.x interface is not compatible with this code.
openai>=1.30,<2

# The three Streamlit apps: rag_chat.py, text_to_sql.py, dashboard.py
streamlit>=1.33,<2

pandas>=2.0
numpy>=1.26
pyarrow>=15.0          # parquet cache for embeddings

python-dotenv>=1.0     # reads the project-root .env
